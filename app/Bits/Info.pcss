.bit {
  color: rgba(0, 0, 0, 0.65);
  font-size: 2.25rem;
  font-weight: 100;
  line-height: 1.1;
  margin: grid(3);
  text-align: center;
}

.sub-text {
  font-size: 1rem;
  line-height: 1.3;
  margin-top: grid(2);
}
