.bit {
  bottom: grid(3);
  left: 50%;
  position: fixed;
  transform: translateX(-50%);

  & button {
    color: #aaa;
  }
}

.wrapper {
  height: 0;
  width: 0;
}

.info {
  left: grid(2);
  bottom: grid(3);
  opacity: .275;
  padding-bottom: 3px;
  position: fixed;
}
